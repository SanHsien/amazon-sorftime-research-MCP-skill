# 高客單長尾

找均價≥$80 且搜尋量適中的關鍵詞（高利潤長尾市場）。

## 工具
`mcp__sellersprite__keyword_miner` — `{"request": {...}}`
`mcp__sellersprite__keyword_research` — `{"request": {...}}`

## 篩選
- 客戶端過濾 `avgPrice >= 80`
- `minSearch`: 500

## 輸出
高客單關鍵詞表 + 搜尋量/供需比/競價
